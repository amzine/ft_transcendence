import React from 'react';

const Footer = (props:any) => {
    return (
        <div className={props.className+" text-3xl text-white flex justify-center items-center"}>
            Ping Pong
        </div>
    )
}

export default Footer